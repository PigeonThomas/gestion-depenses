
version: '3.8'

services:
  adminer:
    image: adminer:4
    container_name: app_adminer
    restart: unless-stopped
    ports:
      - "8081:8080"
    environment:
      ADMINER_DEFAULT_SERVER: db
    depends_on:
      - db
