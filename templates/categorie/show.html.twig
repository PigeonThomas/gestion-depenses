{% extends 'base.html.twig' %}

{% block title %}
	{{ title }}
{% endblock %}

{% block body %}
	<h1>{{ title }}</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ categorie.id }}</td>
			</tr>
			<tr>
				<th>Nom_categorie</th>
				<td>{{ categorie.nomCategorie }}</td>
			</tr>
			<tr>
				<th>Couleur_categorie</th>
				<td>{{ categorie.couleurCategorie }}</td>
			</tr>
			<tr>
				<th>Icone_categorie</th>
				<td>{{ categorie.iconeCategorie }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('app_categorie_index') }}">back to list</a>

	<a href="{{ path('app_categorie_edit', {'id': categorie.id}) }}">edit</a>

	{{ include('categorie/_delete_form.html.twig') }}
{% endblock %}
