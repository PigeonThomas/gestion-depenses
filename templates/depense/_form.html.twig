{{ form_start(form) }}
<div class="row d-flex justify-content-between gap-3">
	<div class="col mt-3">
		{{form_label(form.date_depense)}}
		{{ form_widget(form.date_depense) }}
	</div>
	<div class="col mt-3">
		{{form_label(form.montant_depense)}}
		{{ form_widget(form.montant_depense) }}
	</div>
</div>
<div class="mt-3">
	{{form_label(form.commentaire_depense)}}
	{{ form_widget(form.commentaire_depense) }}
</div>
<div class="row d-flex justify-content-between gap-3">
	<div class="col mt-3">
		{{form_label(form.categorie)}}
		{{ form_widget(form.categorie) }}
	</div>
	<div class="col mt-3">
		{{form_label(form.magasin)}}
		{{ form_widget(form.magasin) }}
	</div>
</div>
<div class="mt-3">
	{{ form_label(form.factureFile)}}
	{{ form_widget(form.factureFile) }}
</div>


{{ form_row(form.user) }}
<div class="text-center">
	{{form_widget(form.save) }}
</div>
{{form_rest(form)}}
{{ form_end(form) }}
